  name: Inputs Demo
  on: 
    workflow_dispatch: 
      inputs:
        environmrnt:
          description: Choose the environment to deploy
          required: true
          default: dev
          options:
            - dev
            - test
            - prod
        artifact_tag:
           description: choose the git tag to deploy
           required: true
  jobs:
    input-demo:
      runs-on: ubuntu-latest
      steps:
        - name: Deploy the App
          run: echo "Deploying the ${{ inputs.artifact_tag }}  applicatiion to ${{inputs.environmrnt}}"
