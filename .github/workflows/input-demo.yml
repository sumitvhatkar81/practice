  name: Inputs Demo
  on: 
    workflow_dispatch: 
      inputs:
        environmrnt:
          description: Choose the environment to deploy
          required: true
          default: "dev"
  jobs:
    input-demo:
      runs-on: ubuntu-latest
      steps:
        - name: Deploy the App
          run: echo "Deploying the applicatiion to ${{inputs.environmrnt}}"
